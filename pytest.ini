[pytest]
# Tell pytest-django which settings module to use.
DJANGO_SETTINGS_MODULE = config.settings
# Run only our tests; ignore virtualenv and cache noise.
python_files = tests.py test_*.py *_tests.py
addopts = -v --reuse-db