# Runtime dependencies (production + dev share these)
# Pinned for reproducible builds.
Django==5.0.6                  # The web framework (ORM required by the task)
djangorestframework==3.15.2    # Serializers, viewsets, browsable API
djangorestframework-simplejwt==5.3.1   # JWT auth (access + refresh)
django-cors-headers==4.4.0     # Allow the Next.js origin to call the API
django-filter==24.2            # Filter tasks by ?date=
Pillow==10.4.0                 # Image support for ImageField
django-environ==0.11.2         # Read config from .env (12-factor)
whitenoise==6.7.0              # Serve static files in production