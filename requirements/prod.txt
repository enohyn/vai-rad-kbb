# Production dependencies: runtime + WSGI server
-r base.txt

gunicorn==22.0.0              # WSGI HTTP server (used on Render; PythonAnywhere uses its own WSGI)
psycopg2-binary==2.9.9        # PostgreSQL adapter (Render free Postgres; not needed for SQLite)
